"""Shared runtime package for Retribalize Lambda images.

Only application-independent code lives here. Application table declarations
stay in the application and are exported to the tooling as ``TABLES``.
"""

__version__ = "0.1.0"
